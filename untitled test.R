# day 1 sample plot
Veremia <- read.csv("veremia_data_full.csv")


#colors
col <- c("black","grey",rainbow(26[4:26])
         
#Plot by species
plot (c(1,3,4,6),as.numeric(viremia[1,6:9]),) type = "1",lwd =2, xlab = "Day Postinfection"

/cloud/
  bloodmeal_for_BIOL3070.csv
blood <-"https://raw.githubusercontent.com/saarman/BIOL3070/main/bloodmeal_for_BIOL3070.csv"
bloodmeal_data <- read.csv(blood)

head (test)
summary (test)


head (bloodmeal_data)

geographic map

```{r}
library(maps)
help(package='maps')
map(database='county',region='utah', col="grey",fill=TRUE, bg="white", lwd=0.05, mar=rep(0,4), border=0, ylim=c(35,45))
```
